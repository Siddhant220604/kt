MONGO_URL=mongodb://localhost:27017/
DB_NAME=kiran_traders
JWT_SECRET=changeme
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Razorpay keys (fill these after creating your Razorpay account)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# WhatsApp Cloud API
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_VERIFY_TOKEN=

WHATSAPP_API_VERSION=v23.0
WHATSAPP_DEFAULT_COUNTRY_CODE=91

# Google Maps server key - address autocomplete, geocoding and delivery distance.
# Needs these APIs enabled on the Cloud project: Places API (New) and Routes API.
# Keep this key server-side; the browser map uses a separate, referrer-restricted key
# (REACT_APP_GOOGLE_MAPS_KEY in the frontend).
GOOGLE_MAPS_API_KEY=
